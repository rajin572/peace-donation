import { FadeLoader } from "react-spinners";

const Spninner = () => {
  return (
    <div className=" isolate aspect-video h-full w-full flex justify-center items-center">
      <FadeLoader color="#fc601d" />
    </div>
  );
};

export default Spninner;
