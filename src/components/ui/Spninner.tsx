import { FadeLoader } from "react-spinners";

const Spninner = () => {
  return (
    <div className=" isolate aspect-video bg-white/20 h-full w-full flex justify-center items-center">
      <FadeLoader color="#fc601d" />
    </div>
  );
};

export default Spninner;
